# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
coverage.xml

# Type checking
.mypy_cache/
.dmypy.json
dmypy.json

# Environment variables
.env
.env.local

# Project-specific
config.yml
config.yaml
*.note
*.bak
temp/
intermediate/
sync_state/

# Logs
*.log
logs/

# OS
Thumbs.db

# Temporary files
tmpclaude-*

# Claude Code IDE
.claude/

# Generated and analysis outputs
examples/analysis/extracted_golden/
examples/analysis/generate_png_note.py
examples/generated/

# project-os cockpit runtime state / local venv
.cockpit/
tools/cockpit/.venv/

# Project inbox — external material staged for triage (LIFECYCLE.md).
# An item is either filed (and the FILED artefact is what gets committed)
# or discarded; committing staging would preserve exactly what triage
# exists to resolve.
# Anchored: an unanchored `inbox/` matches a directory of that name at ANY
# depth, so a `docs/features/inbox/` would vanish from the repository with
# no error anywhere (project-os-cockpit ISS-0070 — a whole feature and three
# tasks were missing from a fresh clone, which then failed the validator).
/inbox/
